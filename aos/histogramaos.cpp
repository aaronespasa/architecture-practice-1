#include <iostream>

#include "aos.h"

void ImageAOS::GenerateHistogram(string filename) {
    std::cout << "filename: " << filename << std::endl;
    // int histogram[256];
    // for (int i = 0; i < 256; i++) {
    //     histogram[i] = 0;
    // }
    // for (int i = 0; i < height; i++) {
    //     for (int j = 0; j < width; j++) {
    //         histogram[bmpPixelsData[i][j]]++;
    //     }
    // }

    // // Write the histogram to a file
    // ofstream file(filename);
    // for (int i = 0; i < 256; i++) {
    //     file << i << " " << histogram[i] << endl;
    // }
    // file.close();
}